<Window x:Class="VD_Toggler_3.RestoreConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Width="480"
        Height="180">

    <Window.Resources>
        <LinearGradientBrush x:Key="ComboTextGradient" EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFEAEB77" Offset="0"/>
            <GradientStop Color="#FFE5E5E5" Offset="0.5"/>
        </LinearGradientBrush>
        <Style x:Key="GlowImageButton"
               TargetType="{x:Type Button}">
            <Setter Property="BorderBrush"
                    Value="{x:Null}"/>
            <Setter Property="Background"
                    Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="Bg"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4"/>

                            <!-- 发光层 -->
                            <Border x:Name="Glow"
                                    Background="Transparent"
                                    CornerRadius="4"
                                    BorderThickness="2"
                                    Opacity="0"
                                    BorderBrush="{StaticResource ComboTextGradient}">
                                <Border.Effect>
                                    <BlurEffect Radius="8"/>
                                </Border.Effect>
                            </Border>

                            <!-- 按钮内容 -->
                            <ContentPresenter Margin="0"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- 悬停：显示发光 -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Glow" Property="Opacity" Value="1"/>
                            </Trigger>
                            <!-- 按下：减弱背景 -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="Glow" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GlowImageButtonMain"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource GlowImageButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid SnapsToDevicePixels="True">
                            <!-- 按钮背景 -->
                            <Border x:Name="Bg"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="14"/>

                            <!-- 发光层 -->
                            <Border x:Name="Glow"
                                    Background="Transparent"
                                    CornerRadius="14"
                                    BorderThickness="2"
                                    Opacity="0"
                                    IsHitTestVisible="False"
                                    BorderBrush="{StaticResource ComboTextGradient}">
                                <Border.Effect>
                                    <BlurEffect Radius="8"/>
                                </Border.Effect>
                            </Border>

                            <ContentPresenter Margin="0"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Glow" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="Glow" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.Foreground>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFEAEB77" Offset="0"/>
            <GradientStop Color="#FFE5E5E5" Offset="0.5"/>
        </LinearGradientBrush>
    </Window.Foreground>
    <Window.Background>
        <ImageBrush ImageSource="/img/cfg/ConfirmBackground.png"/>
    </Window.Background>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image HorizontalAlignment="Left" Height="60" Margin="0,21,0,0"
               Grid.RowSpan="3" VerticalAlignment="Top" Width="60"
               Source="/img/cfg/Question.png"/>
        <TextBlock Text="将所有配置恢复为默认值，此操作不可撤销，是否继续？"
                   TextWrapping="Wrap"
                   FontSize="14"
                   Margin="72,27,0,74"
                   Grid.RowSpan="3"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,45,0,0" Width="190" >
            <Button Style="{StaticResource GlowImageButtonMain}"
                    Content="取消" Width="78" Height="32" Margin="0,0,8,0"
                    IsCancel="True" Click="OnCancel" BorderBrush="{x:Null}">
                <Button.Foreground>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFEAEB77" Offset="0"/>
                        <GradientStop Color="#FFE5E5E5" Offset="0.5"/>
                    </LinearGradientBrush>
                </Button.Foreground>
                <Button.Background>
                    <ImageBrush ImageSource="/img/cfg/ButtonAOk.png"/>
                </Button.Background>
            </Button>
            <Button Style="{StaticResource GlowImageButtonMain}"
                    Content="确认复原" Width="78" Height="32"
                    IsDefault="True" Click="OnConfirm" BorderBrush="{x:Null}">
                <Button.Foreground>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFEAEB77" Offset="0"/>
                        <GradientStop Color="#FFE5E5E5" Offset="0.5"/>
                    </LinearGradientBrush>
                </Button.Foreground>
                <Button.Background>
                    <ImageBrush ImageSource="/img/cfg/ButtonAOther.png"/>
                </Button.Background>
            </Button>
        </StackPanel>
    </Grid>
</Window>